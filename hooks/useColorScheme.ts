/**
 * Re-exports the useColorScheme hook from React Native
 * 
 * This hook provides access to the device's color scheme (light/dark mode).
 * Used for theme-aware styling throughout the application.
 */
export { useColorScheme } from 'react-native';
